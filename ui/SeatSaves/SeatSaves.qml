import QtQuick 2.0
import QtQuick.Controls 2.5
import "../SeatControls"

Rectangle{
    id: seatSaves
    height: heightRatio * 2

    anchors{
        top: parent.top
        left: parent.left
        right: parent.right
    }

    color: "green"

}
